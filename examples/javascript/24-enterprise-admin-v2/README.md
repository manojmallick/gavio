# Enterprise Admin v2 Example

Runs a local control plane and exercises the v2.6.0 enterprise admin workflow:
OIDC metadata, scoped admin keys, approval-gated rollout activation, JSONL audit
export, and retention dry-run/apply.

```bash
node enterprise-admin-v2.mjs
```

No provider key or network access is required.
