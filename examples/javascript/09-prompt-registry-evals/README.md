# 09 - Prompt Registry + Evals (JavaScript)

Shows the v1.4.0 Prompt Registry + Evals foundation: register a versioned
template, render it with lineage, run deterministic eval cases, and verify the
report stores output hashes instead of raw model output.

```bash
npm install
npm start
```

See also: [Prompt Registry + Evals](../../../docs/prompt-registry-evals.md).
