# 20 - Platform Runtime Profile

Builds a metadata-only platform readiness profile for a production Gavio
runtime. The profile summarizes enabled runtime surfaces, exporters,
integrations, controls, and evidence without storing raw prompt or response
content.

```bash
pip install -r requirements.txt
python profile.py
```
