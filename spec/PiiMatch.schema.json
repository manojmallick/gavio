{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gavio.io/schema/PiiMatch.v1.json",
  "title": "PiiMatch",
  "description": "A single detected PII entity within a span of scanned text. start/end are half-open character offsets.",
  "type": "object",
  "required": ["entityType", "start", "end", "value"],
  "additionalProperties": false,
  "properties": {
    "entityType": {
      "type": "string",
      "description": "e.g. EMAIL, IBAN, BSN, CREDIT_CARD, PHONE, IP_ADDRESS, SSN, SECRET",
      "minLength": 1
    },
    "start": { "type": "integer", "minimum": 0 },
    "end": { "type": "integer", "minimum": 0 },
    "value": { "type": "string" },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1, "default": 1.0 },
    "replacement": {
      "type": ["string", "null"],
      "description": "Placeholder used in REDACT mode, e.g. [EMAIL_1]"
    }
  }
}
